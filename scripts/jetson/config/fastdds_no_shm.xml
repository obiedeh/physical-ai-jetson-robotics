<?xml version="1.0" encoding="UTF-8" ?>
<!-- UDP-only FastDDS profile for the ROSMASTER M3 Pro Orin: the vendor
     micro-ROS agent runs as user `jetson`; its shared-memory segments are
     not joinable by another user, so a second account (oedeh) never
     discovers /YB_Node unless SHM is disabled on our side. -->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <transport_descriptors>
    <transport_descriptor><transport_id>udp_only</transport_id><type>UDPv4</type></transport_descriptor>
  </transport_descriptors>
  <participant profile_name="udp_only_participant" is_default_profile="true">
    <rtps><userTransports><transport_id>udp_only</transport_id></userTransports><useBuiltinTransports>false</useBuiltinTransports></rtps>
  </participant>
</profiles>
