diff --git a/isaac/scripts/ludo_turn_executor.py b/isaac/scripts/ludo_turn_executor.py
index d0d9e97..3c45a71 100644
--- a/isaac/scripts/ludo_turn_executor.py
+++ b/isaac/scripts/ludo_turn_executor.py
@@ -1486,16 +1486,17 @@ def _run(args: argparse.Namespace) -> int:
         _dev = env.device
         _ph = torch.full((1,), IDLE, dtype=torch.long, device=_dev)
         _def = robot.data.default_joint_pos[:, arm_ids].clone()
+        _up = torch.zeros(1, 3, device=_dev); _up[:, 2] = 1.0
         za = torch.zeros(1, 7, device=_dev); za[:, 6] = OPEN_CMD
         for _ in range(40):
             wrapper.step(za)
-        hq0 = robot.data.body_quat_w[:, tool_idx].clone()
+        GZ = TABLE_SURFACE_Z + 0.012      # die grasp height (18mm die)
 
-        def servo(tx, ty, tz, grip, steps, tol=0.008, ori_w=0.8):
-            at = robot.data.joint_pos[:, arm_ids].clone()
+        def servo(tx, ty, tz, grip, steps, tol=0.008, cap=True):
             tgt = torch.tensor([[tx, ty, tz]], device=_dev, dtype=torch.float32)
+            at = robot.data.joint_pos[:, arm_ids].clone()
             for _ in range(steps):
-                dq = dls_pos_step(tgt)  # position-only: orientation free, arm can reach
+                dq = level_dq(tgt, _up, _ph)   # position + keep gripper LEVEL
                 at = at + dq
                 meas = robot.data.joint_pos[:, arm_ids]
                 at = torch.clamp(at, meas - 0.55, meas + 0.55).clamp(
@@ -1504,46 +1505,42 @@ def _run(args: argparse.Namespace) -> int:
                 a[:, :6] = at - _def
                 a[:, 6] = grip
                 wrapper.step(a)
+                dump_frame("dice-in")
                 gc = mdp._grasp_center_local(env)
-                if float(torch.norm(tgt - gc)) < tol:
+                if cap and float(torch.norm(tgt - gc)) < tol:
                     break
 
         ok = 0
         for k in range(args.dice_in):
             d = die_obj.data.root_pos_w[0].cpu().numpy()
             c = cup_obj.data.root_pos_w[0].cpu().numpy()
-            servo(d[0], d[1], d[2] + 0.06, OPEN_CMD, 150)          # above die
-            servo(d[0], d[1], d[2] + 0.004, OPEN_CMD, 150, tol=0.006)  # to die
+            servo(d[0], d[1], GZ + 0.09, OPEN_CMD, 200)            # above die, level
+            servo(d[0], d[1], GZ, OPEN_CMD, 200, tol=0.006)        # descend to die
             gc = mdp._grasp_center_local(env)[0].cpu().numpy()
-            print(f"[dicein] att{k+1} at-die: gc={gc.round(3).tolist()} "
-                  f"die={d.round(3).tolist()} gc_err={_np.linalg.norm(gc-d)*1000:.0f}mm "
+            print(f"[dicein] att{k+1} at-die gc_err={_np.linalg.norm(gc-_np.array([d[0],d[1],GZ]))*1000:.0f}mm "
                   f"width={float(width_now()[0])*1000:.0f}mm", flush=True)
-            for _ in range(40):                                    # close
+            for _ in range(50):                                    # close on die
                 a = torch.zeros(1, 7, device=_dev)
                 a[:, :6] = robot.data.joint_pos[:, arm_ids] - _def
-                a[:, 6] = CLOSE_CMD; wrapper.step(a)
-            print(f"[dicein] att{k+1} post-close width={float(width_now()[0])*1000:.0f}mm "
-                  f"die_z={float(die_obj.data.root_pos_w[0,2]):.3f}", flush=True)
-            servo(d[0], d[1], d[2] + 0.16, CLOSE_CMD, 150)         # lift
+                a[:, 6] = CLOSE_CMD; wrapper.step(a); dump_frame("dice-in close")
+            servo(d[0], d[1], GZ + 0.16, CLOSE_CMD, 200)           # lift
             print(f"[dicein] att{k+1} post-lift die_z={float(die_obj.data.root_pos_w[0,2]):.3f} "
-                  f"(table={TABLE_SURFACE_Z:.3f})", flush=True)
-            servo(c[0], c[1], c[2] + 0.16, CLOSE_CMD, 200)         # over cup
-            servo(c[0], c[1], c[2] + 0.09, CLOSE_CMD, 100)         # lower to rim
-            for _ in range(60):                                    # release
+                  f"(table {TABLE_SURFACE_Z:.3f}) width={float(width_now()[0])*1000:.0f}mm", flush=True)
+            servo(c[0], c[1], c[2] + 0.16, CLOSE_CMD, 250)         # over cup
+            servo(c[0], c[1], c[2] + 0.09, CLOSE_CMD, 120)         # lower to rim
+            for _ in range(70):                                    # release
                 a = torch.zeros(1, 7, device=_dev)
                 a[:, :6] = robot.data.joint_pos[:, arm_ids] - _def
-                a[:, 6] = OPEN_CMD; wrapper.step(a)
-            for _ in range(120):                                   # let it settle
-                wrapper.step(za)
+                a[:, 6] = OPEN_CMD; wrapper.step(a); dump_frame("dice-in release")
+            for _ in range(120):
+                wrapper.step(za); dump_frame("dice-in settle")
             dp = die_obj.data.root_pos_w[0].cpu().numpy()
             cp = cup_obj.data.root_pos_w[0].cpu().numpy()
             r = float(_np.hypot(dp[0] - cp[0], dp[1] - cp[1]))
             inside = (r < 0.016) and (dp[2] > cp[2]) and (dp[2] < cp[2] + 0.05)
             ok += int(inside)
             print(f"[dicein] attempt {k+1}: {'IN ' if inside else 'MISS'} "
-                  f"die_r_from_cup={r*1000:.0f}mm die_z-cup_z={(dp[2]-cp[2])*1000:.0f}mm",
-                  flush=True)
-            # reset die to its start for next attempt
+                  f"r={r*1000:.0f}mm dz={(dp[2]-cp[2])*1000:.0f}mm", flush=True)
             root = die_obj.data.default_root_state.clone()
             die_obj.write_root_pose_to_sim(root[:, :7])
             die_obj.write_root_velocity_to_sim(torch.zeros_like(root[:, 7:]))