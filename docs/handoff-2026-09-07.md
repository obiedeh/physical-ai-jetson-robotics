# Handoff — 2026-09-07

Current resumption note. Supersedes the September 7 scratchpad HTML summary of
August 19–27 work where results or prerequisites conflict. Historical ledgers
and raw artifacts remain unchanged. Read the [current README](../README.md)
and [evidence limits](../reports/NOT_CLAIMED.md) alongside this note.

## Repository snapshot

- Reviewed at `main`, `d84cdd9`, five commits ahead of the locally recorded
  `origin/main`; no remote fetch was performed.
- Vendor removal and evidence/documentation corrections are uncommitted.
  The manifest and staging script were committed in `c425b2a`; that commit
  alone does not include asset removal. Skill hashes are in `d84cdd9`.
- Run directories track provenance, session summary and turns records;
  frames, per-frame metadata, per-run video, raw corpora and checkpoints are
  ignored and not
  distributed (README, Data availability). Local existence of those does not
  establish availability in a fresh clone.
- Update 2026-09-09: the public repository starts from a clean single commit; the
  earlier history lives in the private `physical-ai-jetson-robotics-history`.
- (Original note) Removed vendor assets remain in the pre-publication history. Current-tree cleanup does
  not remove that history; see the [removal manifest](VENDOR_ASSET_REMOVAL.md).

## Results and limits

| Track | Evidence-supported status | Unresolved work |
| --- | --- | --- |
| Ludo expert | Selected Isaac Sim seeds 10–12: 35/36 turns with one retry, 32/36 first tries. Includes PPO reaching, scripted behavior, kinematic attach and release-hold. | Keep scoring/configuration and simulation limits attached to every aggregate. |
| Ludo dice/game ladder | Separate game-to-winner demo and integrated simulation video with three dice rolls and three turns. | Neither establishes a complete physically rolled game to a winner or real-robot manipulation. |
| GR00T eval01 | **0/20 corrected**. The old apparent success never grasped, lifted or released the cup. The goal was unobservable. | Target-blind placement is not a fair goal-conditioned comparison. |
| GR00T eval02 | Target-observable language run recorded [0/20](../reports/ludo_stats_frozen/stats_2026-08-21T175006Z_reports_ludo_groot17_eval02_tobs.json). | These runs do not establish language conditioning as the sole cause of failure. Target-in-state remains proposed. |
| M3 Pro recording | August 20 record verifies Orin host / RTX keyboard recording end to end; human demonstrations remain pending in that record. | Operator-present collection with observed object-success evidence; command-derived arm state is insufficient. |
| V2D | August 21 feasibility record identifies reconstruction incompatibility on the inspected stack and a separate Stage 3 mocap path. | Recheck checkout, compatibility and inputs; historical feasibility is not current runtime validation. |
| Thor / Orin | Short inference and device probes exist. | Numerical parity, sustained thermal behavior and safety qualification remain unestablished. |

Sources: [GR00T correction and scope](notes/groot17_eval01_findings_2026-08-20.md),
[corrected sidecar](../reports/ludo_groot17_eval01/session_summary_corrected.json),
[Ludo milestones](notes/ludo_game_milestones_2026-08-21.md),
[recording and strategy](notes/yahboom_strategy_2026-08-20.md),
[V2D feasibility](notes/video_to_data_layer_plan_2026-08-21.md).

## Prerequisites before simulation

Vendor-dependent Isaac tasks cannot run with the removed assets absent.
Retained reference/patch layers do not replace their source geometry. Use the
[vendor setup instructions](VENDOR_INTEGRATION_MAP.md#setup) and
`python3 scripts/fetch_vendor_assets.py --help`.

The local Synria reference at `~/models/reference/vendor_descriptions/synriard`
passed the staging script's in-memory source-plan validation: 10 source files,
21 planned destination files. No assets were installed into this worktree. This checks expected
structure and mesh availability, not historical identity, USD composition or
runtime behavior. The original upstream revision remains unverified.

Before resuming, establish the source identifier, validate staging with
`--check`, then stage into the intended local environment and regenerate
required USD layers. Account for external game-table scenes and Robotiq inputs
separately. Record source hashes and the resulting configuration. Coordinate
staging with the pending removal review so restoring tracked paths does not
accidentally erase the deletion patch.

After assets and composition validate, use the retained Ludo runbook.
Integrated runs use `--headless`: a recorded GUI-crash workaround, not proof
of an underlying repair. Review the launcher's shared-memory cleanup against
other active Isaac sessions before using it.

## Work order

The strategy remains **B → A → C → D**: on-device policy evidence, shared
embodiment evaluation, measured safety behavior, then a sim-to-real gap ledger.
These are planned deliverables, not completed hardware capabilities.

1. Local cleanup and CI audit completed (evidence below). The patch remains
   uncommitted; retain raw reports and the distinction between optional-asset
   skips and integration passes when reviewing it for merge.
2. Prepare operator-present Leg B demonstrations through the recorded keyboard
   path. Training on real demonstrations depends on collection; independent
   simulation/data preparation does not.
3. For GR00T target-in-state, align offline target xy augmentation, 10D state
   names, modality slices, conversion and evaluation observations. Validate
   dataset loading and coordinate agreement before the existing pre-GPU
   checkpoint. Use the corrected scorer. Another negative result alone would
   not prove data scale is the cause.
4. For V2D, verify Stage 3 example inputs/environment, then LOAD → retarget/IK
   → play. Sharpa-to-Synria retargeting remains separate work. A play test
   alone would not establish an RL improvement.
5. After real demonstrations exist, train and measure on-device serving, then
   proceed to Legs A/C/D with the strategy record's evidence requirements.

## Completed cleanup and validation

The continuation audit found and fixed:

- Default pytest discovery collected standalone GPU/hardware diagnostics.
  Test roots now explicitly include `tests`, `game_core` and `ludo_engine`;
  the dev extra declares their missing `python-chess` dependency.
- Optional-asset skips hid the first-party Synria wrapper checks and coupled
  the two vendors' structural checks. Those checks now run independently;
  a present but incomplete M3Pro package fails manifest validation.
- ROS CI selected removed vendor packages and omitted the first-party control
  package needed by bringup. The four first-party packages now build locally.
- The GR00T dry-run CLI forwarded obsolete epoch/batch flags. It now forwards
  `--max-steps` and `--global-batch-size`, matching the recorded G3 migration
  and inspected local GR00T config. Tests and runnable examples use
  `new_embodiment` and wrist/overhead; obsolete dataset-helper tests now verify
  missing-dataset rejection through the actual training entry point.
- Lint/type failures were corrected with annotations/import formatting.
  The sweep script imports the existing pad offsets instead of duplicating
  their literals; values and control thresholds are unchanged.

| Acceptance check | Observed evidence |
| --- | --- |
| Full Python suite | **676 passed, 19 skipped** using the declared test roots and an isolated environment with the chess dependency. 18 skips are absent vendor assets; one is the guard for environments without rclpy, which is installed here. |
| Independent clean snapshot | **676 passed, 19 skipped**, with untracked reports excluded. Lint and mypy also passed in that copy. |
| CI lint and types | Exact workflow lint scope passed; mypy passed on **45 source files**. |
| ROS Jazzy build | **4 packages built**: rosmaster_m3pro_description, rosmaster_m3pro_control, rosmaster_m3pro_bringup, synria_arm_description. Build outputs isolated under `/tmp`. No launch or hardware test. |
| Vendor removal | All **86** actual deletions match the manifest; all 86 paths are covered by ignore rules. |
| Optional vendor installation | In the temporary snapshot, both staging dry-runs, installs, receipt hashes and overwrite refusal passed. Synria used the local unpinned reference; Yahboom used historical repository files as test fixtures, not newly verified upstream inputs. |
| Asset checks with fixtures | **48 passed**, with no skips. Removing M3Pro package.xml correctly failed; malformed Synria also failed while Yahboom was absent. Restoring fixtures returned **48 passed**. |
| Runtime-preserving maintenance | Separate AST comparison against HEAD confirmed unchanged runtime bodies/values in six files after ignoring annotation/import formatting. The shared pad-offset values match the removed duplicate. |
| Requested Codex setting | Parsed the local TOML and verified top-level `approvals_reviewer = "auto_review"`; no config edit was needed. |

Reproduce Python validation after installing the dev extra:

```bash
python -m pytest -q
ruff check physical_ai_lab arm_control edge_ai slam agents lerobot isaac/isaaclab_tasks tests
mypy physical_ai_lab arm_control edge_ai slam lerobot isaac/isaaclab_tasks --ignore-missing-imports
```

These checks cover local software/asset contracts, not Isaac simulation or
real-robot operation. Initial unrestricted collection executed a standalone
CUDA diagnostic before failing; the corrected discovery excludes those scripts.
No training, robot command, live-worktree asset restoration, commit, push or
visibility change was performed. Windows and other Python matrix jobs were not
executed locally. Source revisions, USD regeneration, physical success, numerical
parity and sustained safety/thermal validation remain the future-work limits
above. Claude Code review before merge remains required by [AGENTS.md](../AGENTS.md);
local validation is not a production-readiness or hardware-safety approval.
