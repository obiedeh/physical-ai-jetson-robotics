# Handoff - 2026-07-28 23:44 CDT

The GR00T track runs in its own implementation session, fully separate from
the RL track, so the two approaches can be compared honestly. Shared
surface: the simulator scene and the fixed evaluation harness
(`isaac/scripts/eval_synria_sequence.py`) — nothing else.

## State as of handoff

- **Cloned**: `~/Isaac-GR00T` (NVIDIA/Isaac-GR00T, shallow clone).
- **Venv**: `~/.venv/gr00t` (bare — pip/setuptools/wheel only).
- **Install FAILED at flash-attn**: it needs torch preinstalled in the
  venv before building. Fix, in order:
  1. `~/.venv/gr00t/bin/pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cu128`
     (Blackwell/RTX 5090 needs cu128 — the cu126 default breaks).
  2. `cd ~/Isaac-GR00T && ~/.venv/gr00t/bin/pip install -e . --no-build-isolation`
     (no-build-isolation so flash-attn sees the installed torch).
  3. Model weights: `huggingface-cli download nvidia/GR00T-N1.5-3B`
     (verify the current model name in the repo README; the scaffold
     targets the N1.7-era API names and may need light edits).
- **Scaffold** (written earlier, import-tested):
  `isaac/isaaclab_tasks/synria_pickplace/gr00t/` — embodiment config,
  LeRobot dataset adapter, finetune + inference entries.
- **Recipe**: `docs/SYNRIA_GR00T_FINETUNE.md` (550 lines, end-to-end).

## Plan for the GR00T session

1. Finish the install (above) — CPU/network only.
2. **Cameras**: add wrist + overhead camera sensors to a recorder
   variant of the env (GR00T is vision-based; the RL task is
   state-based — do NOT modify the RL task's observation space).
3. **Demonstrations**: ~100 LeRobot-format demos. The attach-on-grasp
   event (E8, `mdp.attach_carried_pieces`) makes the hybrid expert
   (`isaac/scripts/record_synria_demos.py`, policy-picks + scripted
   release/retreat) viable as a scripted demo source; teleop is the
   higher-quality alternative.
4. **LoRA fine-tune** per the recipe (Path A first; GR00T-Mimic
   amplification later if needed).
5. **Comparison**: wrap GR00T inference as a policy callable and run
   THE SAME fixed harness (seed 123, 256 envs where VRAM allows, same
   stage metrics) against the RL track's ledger numbers.

## Ground rules (from the RL track's hard lessons)

- GPU is shared with RL training runs in the other session — coordinate
  heavy GPU phases (fine-tune ~10-24 GB) around the RL runs (~6-9 GB).
- The gripper-fidelity blocker (see `docs/SYNRIA_EXPERIMENT_LEDGER.md`,
  amended diagnosis) applies to GR00T too: contact-physical carries and
  releases are unreliable in the current model. Record demos WITH the
  attach event enabled, and note the caveat in any comparison.
- Never disable collision checks, joint limits, velocity limits,
  workspace bounds, or gripper-force limits.
- pkill self-match footgun: use char-class patterns like
  `pytho[n]` / `train_synria_pickplac[e].py`, and never combine a kill
  with a launch in one command.

## Independence contract (2026-07-29, user-mandated)

The two sessions must never block each other's goals:

1. **Filesystem**: RL track works ONLY in
   `~/github/physical-ai-jetson-robotics` (branch `main`); GR00T track
   ONLY in `~/github/physical-ai-jetson-robotics-gr00t` (branch
   `gr00t-integration`) plus `~/Isaac-GR00T` and `~/.venv/gr00t`.
   Neither edits the other's tree.
2. **Git**: separate branches; merges to `main` are deliberate,
   never automatic. Docs/ledger updates land on `main`; the GR00T
   session pulls them when it chooses.
3. **GPU budget**: RL track commits to <= ~10 GB (its 4096-env runs use
   6-9 GB), leaving >= 20 GB for GR00T fine-tuning on the 32 GB 5090.
   If either session OOMs, it downsizes its own job (RL: fewer envs;
   GR00T: batch size / LoRA rank) — it does NOT ask the other to stop.
4. **Processes**: neither session kills processes it did not start.
   Kill patterns must match the session's own script + args exactly
   (RL uses `train_synria_pickplac[e].py ... num_envs 4096`); never
   bare `pytho[n]` or `isaacsim` patterns.
5. **Artifacts**: RL checkpoints live in this tree's
   `reports/training/synria_chess_pickplace_v1/`; GR00T datasets,
   checkpoints, and logs live in ITS worktree's `reports/`.
6. **Shared truth**: the fixed evaluation harness
   (`isaac/scripts/eval_synria_sequence.py`, seed 123 protocol) is the
   single comparison instrument; both tracks report against it, neither
   modifies it without noting the change in the experiment ledger.
